# IC-20260812-021 自验报告

## 一、当前结论

规格到 XCTest 的可追溯矩阵已按当前 179 个测试方法重跑。候选矩阵在写入前及文本回读后均通过迁移集合一致性闸门：115 个迁移单元格坐标与旧矩阵完全一致，63 个不可达标记也与旧矩阵完全一致。Windows 静态自验与 macOS CI 均已完成；179 项 XCTest 全部通过。

## 二、前置条件与输入证据

| 项目 | 核对结果 |
|---|---|
| 重跑前仓库状态 | `main` 与本地跟踪的 `origin/main` 同指向 `c7ed4b18c579fd6d2904ea856dcac404906e2ec0`，工作树干净 |
| IC-20260812-019 受验实现 | `01ebf8263d20a58340aedd78be98cadd06d30eb0`，是重跑基线祖先 |
| IC-20260812-019 推送证据 | 重跑基线已位于本地跟踪的 `origin/main`；上游报告记录 CI #16 成功、179 项 XCTest 全绿 |
| 旧矩阵 Git blob | `a3dcdb21a5932c32de01938bb18b60d432404083` |
| S3-S4 规格 | `SPEC-S3-S4-20260812.v6.md`；SHA-256：`BF52BBE87692A253BDA9C2AC8B55712C76AB453E3AAF6C5D286BC15835E04C7D` |
| S5 规格 | `SPEC-S5-20260812.v5.md`；SHA-256：`10CD2B7829126ABBD8FB66091B21169E698868CA67E345D0EABBA39D8D6221B7` |

## 三、重跑方法与判定口径

- 沿用 IC-20260812-011 的 376 条机械提取结果、七个正向字段、四个反向字段、逐字原文核对与双向映射方法。
- 从当前八个 XCTest 源文件静态提取 179 个唯一方法；29 个方法名自带条款编号，按编号形成直接映射。
- `TransitionTableGuardTests` 从测试资源读取矩阵；只把该方法映射到 63 个“断言型条款”，不把单纯遍历的 52 个可达单元格视为行为覆盖。
- 条款依赖尚未实现的 S1、S2、上游整理页、清理入口页或照片列表页时，判为“不适用：MVE 范围外”，不判“未覆盖”。脚本从 `README.md`、`UNDECIDED-ITEMS.md`、实际功能目录与条款语义共同推导出 18 条。
- 第五类不适用不是按题卡编号直接写入。脚本检查 S3 资产、缓存、状态与冻结快照的私有写权限，六个变更/冻结入口均同步归一化，`S3-2` 与 `canSubmit` 的门禁，S4 只能从冻结结果启动，以及冻结失败时删除服务零调用；证据全部成立后，再从条款原文与迁移坐标机械命中 `C34-052`、`C34-090`。

## 四、矩阵统计

| 指标 | 数量 |
|---|---:|
| 条款总数 | 376 |
| 已覆盖 | 265 |
| 未覆盖 | 72 |
| 不适用 | 39 |
| 第五类 | 2 |
| MVE 范围外 | 18 |
| 纯文案 | 12 |
| 纯视觉 | 6 |
| 未定项阻断 | 1 |
| XCTest 方法 | 179 |
| 未命中测试 | 8 |

三张规定汇总表位于 `Reports/TRACEABILITY-S3-S5.md` 的第五至第七节：未覆盖清单 72 条、不适用清单 39 条、未命中测试清单 8 项。正向矩阵全表不在本报告重复展开。

## 五、坐标与标记一致性

| 比对项 | 旧矩阵 | 重跑矩阵 | 结果 |
|---|---:|---:|---|
| 迁移单元格坐标集合 | 115 | 115 | 集合完全一致 |
| 不可达标记集合 | 63 | 63 | 集合完全一致 |
| 可达标记集合 | 52 | 52 | 由前两项差集复核一致 |

一致性检查在写文件之前执行；脚本还会把候选文本重新解析，再做一次独立集合比较。任一集合不同或当前文件不是旧矩阵/同一候选矩阵时，脚本退出 1，不覆盖矩阵。

## 六、自验脚本与执行结果

生成候选并在闸门通过后写入：

```powershell
& ./Scripts/verify-IC-20260812-021.ps1 -写入矩阵
```

执行结果：退出码 0，共执行 3,706 项检查。统计为条款 376、已覆盖 265、未覆盖 72、不适用 39、第五类 2、XCTest 方法 179、未命中测试 8；坐标 115/115、不可达标记 63/63，两个集合均完全一致。

最终只读重算：

```powershell
& ./Scripts/verify-IC-20260812-021.ps1
```

执行结果：退出码 0，共执行 3,715 项检查；脚本从旧矩阵 Git blob 与当前 179 个测试方法重新生成候选，并确认当前矩阵与候选逐字一致。

## 七、范围审查

- 产品代码：未修改。
- 测试代码：未修改。
- SPEC 文件：未修改。
- 未覆盖条款：未给出修复建议或优先级。
- 网络与账号：重跑和本地自验未联网、未操作账号；仅按题卡许可推送本仓库，并只读查看该次 CI 结果。
- 本卡允许改动仅为追溯矩阵、自验脚本、自验报告和让 CI 调用本卡自验脚本的一行工作流更新。

## 八、全量 XCTest 与 CI

- 受验矩阵提交：`72505cd0b8cecc24704d5eb209b0d7cea6d05285`。
- CI：`iOS 构建与自验 #17`，运行 ID `31654178772`，状态成功，总耗时 3 分 49 秒。运行链接：[重跑 S3-S5 XCTest 可追溯矩阵 #17](https://github.com/a734462653-design/PhotoCleanupMVE/actions/runs/31654178772)；任务链接：[构建、XCTest 与未签名产物](https://github.com/a734462653-design/PhotoCleanupMVE/actions/runs/31654178772/job/94304841174)。
- 本卡静态自验步骤成功，耗时 3 秒。
- XCTest 步骤耗时 2 分 40 秒；共执行 179 项，0 失败、0 unexpected，日志终态为 `TEST SUCCEEDED`。
- `testAll115TransitionCellsAndEveryUnreachableCombination` 单独通过，0.015 秒；因此运行时读取重跑矩阵的守卫测试仍全绿。
- 未签名 Release 构建与产物上传均成功；本次运行生成 1 个可下载产物。
- 本报告的 CI 回填提交只修改报告并使用 `[skip ci]`；其最终哈希由任务回传给出，受验矩阵、脚本、工作流和测试均保持为上述 CI 已验证版本。
