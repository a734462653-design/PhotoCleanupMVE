# IC-117 自验报告：原生 Liquid Glass 实装——**绿**（含三卡总执行结果包）

## 结论（先行）

**IC-117 绿，一次 CI 通过。** 被测提交 = `d679bc82205ad4559c778f41e2e8669ef7d351cf`，
CI **#225**（run 33309243215）success，**XCTest 575 项 0 失败**，真实退出码 0，
IPA 1038132 字节，SHA-256 `eece8547a935ddc4aa2ea6ece274b39e80679162f6688d2be227630e58421289`，
工具链 Xcode 26.3（`/Applications/Xcode_26.3.0.app`，`ls` 实证步照常打印）。
CI 预算用 **1/2**。`schemaVersion == 7`、登记值、冻结三链、`Scripts/`、`ci.yml` 本卡零改动。
**未合并 `main`，执行完即停。**

**重要边界（①）**：CI 的 XCTest 跑在 **iOS 18.5 模拟器**（`test-xcode.sh` 既有目的地选择），
故 iOS 26 玻璃分支仅由 **SDK 26.2 完成编译验证**，运行时未执行；卡内第 2 条担心的
"iOS 26 模拟器下像素探针因玻璃渲染差异失败"**未被触发也未被检验**——探针实际全程走回落配方。
玻璃观感全部保留给 H52 真机判定，不代为下结论。

## 输入与范围

- 输入：原单 `DISPATCH-20260830-115-117.md` IC-117 节（范围 = IC-114 子项 A 第 1/2 条原文）+ v2 联动单收尾条款。
- 继承提交：`162966b`（IC-116 报告 docs 提交；开工时工作树净）。
- 目标分支：`feature/ic-110-visual-batch`。
- 范围边界：仅 `PhotoCleanupMVE/Features/S2/S2View.swift`（+82/−13，一个 commit）。

## 实装内容（commit `d679bc8`）

1. **玻璃族单入口分流**：`s2ChromeGlassBackground(in:interactive:)` 改为 `@ViewBuilder` 双分支——
   iOS 26+ 走 `glassEffect(.regular, in: shape)`（`interactive: true` 时 `.regular.interactive()`）；
   **手写白底/内外描边/高光在该分支一概不挂**，质感交给系统。原配方一字不动移入
   `s2LegacyChromeGlassBackground`，iOS 17–25 经 `#available` 回落（`S2ChromeGlass` 全部常量保留）。
2. **按形状**：圆钮 `Circle()`、胶囊 `Capsule()`、中央指示容器 `Capsule()` 均按原形状传入。
3. **圆钮交互变体**：`s2ChromeCircleGlass` 传 `interactive: true`，覆盖顶部返回/垃圾桶、
   底部心形/加相簿四枚圆钮。胶囊与中央容器为常规变体。
4. **同排包 `GlassEffectContainer`**：顶排（`S2TopBarLayout` 三件）与底排（`HStack` 三件）在
   iOS 26 各包一层；17–25 直接返回原行视图（拆出 `topBarRow` / `actionBarRow`，18.5 下视图树不变）。
5. **前景走系统 vibrancy**：新增 `S2ChromeForeground`——iOS 26 上顶部胶囊副行改 `.secondary`、
   中央指示的文字与图标改 `.primary`（不自设透明度）；回落分支保留画布定值（白 62% / 纯白），
   `subtitleOpacity == 0.62` 常量断言不受扰。
6. **A3 过渡动画未触碰**：`s2ChromeVisibilityTransition` 与 `S2ChromeVisibilityTransition` 零 diff。
   与系统玻璃自带过渡是否冲突只能真机观察（H52 第 3 项）；若冲突按卡内"以系统观感为准"另卡登记参数。

## 验收门禁逐条

| 门禁 | 结果 | 依据 |
|---|---|---|
| CI success、0 失败、退出码 0 | ✅ | #225：575 项 0 失败；job success，`exit "$test_status"` ⇒ 0 |
| 既有像素探针不因玻璃失败 | ✅（未触发） | 探针跑在 iOS 18.5 回落配方上，逐值未变；iOS 26 渲染差异未被检验（如上边界声明），未调任何探针阈值 |
| `schemaVersion == 7`、登记值、冻结三链、`Scripts/` 零改动 | ✅ | 本卡仅动 `S2View.swift`；冻结三链 tip 未动 |
| 手写白底/描边/高光在 iOS 26 分支全删 | ✅ | `glassEffect` 分支无任何 fill/strokeBorder 叠层（代码可核） |

## 登记与取定（报告制）

1. **教程提示卡随族同变**：卡面枚举"顶部三件、底部三件、中央指示容器"共 7 件，但 IC-113 A（④）
   已把教程提示卡并入同一玻璃族并明令"全族一套值，不许各件各调"。本卡在族入口分流，提示卡在
   iOS 26 下随族获得 `glassEffect(RoundedRectangle 20)`。拆出去单独维持旧配方 = 违背 IC-113 A ④，
   故按族同变处理，此为登记项，观感由 H52 兜底。
2. **交互变体只给圆钮**：底部"加入最近相簿"胶囊虽可点，卡面明示"圆钮加交互变体"，胶囊保持常规。
3. **未动的自设视觉值（非图标/文字）**：中央指示分隔线白 35%、已标记块白 16% 圆底、
   垃圾桶教程态白底外发光（IC-112 C 取定）——均为内容/教程视觉而非玻璃配方或文字透明度，保留原值。
4. `S2ChromeGlass` / `S2ChromePillMetrics` 常量全部保留（回落配方 + 测试契约），无出厂值变更，
   `schemaVersion` 不递增。

## 本地门禁（①）

提交前均退出码 0：`git diff --check`、`Scripts/selfcheck.ps1`、`Scripts/scan-hardcoded-user-visible-strings.ps1`。

---

# 三卡总执行结果包（IC-115 v2 → IC-116 → IC-117，全绿）

| 卡 | 代码提交 | CI | XCTest | 真实退出码 | IPA 字节 | IPA SHA-256 |
|---|---|---|---|---|---|---|
| IC-115 v2 | `d1fbad6` | **#223**（33306565181） | 575 / 0 | 0 | 991043 | `7bfce132468cf9d18efe5752845efdd595f2ffebdc2d58e8c34ee8c40ea1bb92` |
| IC-116 | `d81c6a6` | **#224**（33308193560） | 575 / 0 | 0 | 1018330 | `3a0e40b9a4baf841078ed845dc54544357f594592a44604518bc5d6e56704098` |
| IC-117 | `d679bc8` | **#225**（33309243215） | 575 / 0 | 0 | 1038132 | `eece8547a935ddc4aa2ea6ece274b39e80679162f6688d2be227630e58421289` |

- **最终绿 tip（代码）= `d679bc8`**；分支最终 tip 为其后的 IC-117 报告 docs 提交（`paths-ignore` 不触发 CI，预期）。
- **H52 包 = `d679bc8`，run #225**（Xcode 26.3 工具链构建，iPhoneOS26.2.sdk）。
- CI 用量：IC-115 v2 1/2、IC-116 1/2、IC-117 1/2，合计 3 次。
- 工具链：#224 起 Xcode 26.3（Build 17C529）；XCTest 模拟器 iPhone 16 / iOS 18.5（见停线/偏差第 2 条）。
- **未合并 `main`**（仍 `a013098…`）；冻结三链未触碰；`schemaVersion == 7` 全程未变。

## H52 人工判定清单（保留给 Lynn，不代为下结论）

1. 玻璃与系统 Photos 并排（透镜质感、按压反馈）——**本包首次可判**（iOS 26 真机走原生玻璃分支）。
2. 放大自动隐藏：捏合/双击进入即隐藏（带 5b 动画），退出恢复进入前状态；单击语义不变。
3. 隐藏过渡动画（显→隐放大模糊淡出、隐→显反向）；**兼看与系统玻璃自带过渡是否冲突**（冲突则按卡取系统观感、报参数）。
4. H51 已判四项抽查回归（教程步 2/4/5、相簿选择器与新建、中央指示、残影）。
5. 132 条隐藏态手势回归。
6. **截图资产放大回归**（v2 联动单新增）：进入放大瞬间跳基准照旧、Nx 期间无异常残留、退出后 chrome 与截图几何按恢复的 V 一致落地。

## 停线 / 偏差 / 待补核（逐条）

### 停线
- 三卡均未触发停线。IC-115 v1 的停线由 v2 裁定解除（判据分段，三条否决维持）。

### 偏差
1. **IC-116 的 `ls` 实证以运行时步骤实现**：卡面"先 `ls` 实证再写路径"落地为 ci.yml 步骤内
   先 `ls /Applications` 打印、再按版本序选实际存在的最高 26.x 稳定版写 `DEVELOPER_DIR`——
   版本号完全来自 runner 实时 `ls`（#224 选中 26.3.0），非预先硬编码。若决策会话要求改为
   字面路径硬编码，#224 日志中的 `ls` 输出即为写路径依据。
2. **XCTest 仍跑在 iOS 18.5 模拟器**：`test-xcode.sh` 取第一个 iPhone 目的地、`Scripts/` 零改动授权，
   故工具链虽切 26.3，测试运行时仍为 18.5——iOS 26 运行时行为差异与玻璃渲染差异均未被 CI 检验。
   如需测试跑 iOS 26 模拟器，须另卡授权改 `test-xcode.sh` 目的地选择。
3. **教程提示卡随玻璃族同变**（IC-117 登记第 1 条）：卡面枚举 7 件之外的第 8 件，因 IC-113 A ④ 随族处理。

### 待补核
1. IC-114 遗留两项仍有效：A3 静止态是否影响玻璃透光（iOS 26 原生分支下 A3 的 blur/scale 恒等值
   与系统玻璃的叠加观感须真机看）；Nx 期间手动切 V 后退出按记录值恢复的取定是否符合预期。
2. H52 第 3 项若发现 A3 与系统玻璃过渡冲突：按卡内"以系统观感为准"处置并登记参数（须新卡授权改动）。
3. iOS 26 真机上前景 vibrancy（`.primary`/`.secondary`）与画布定值的观感差异，由 H52 第 1 项一并判。
