# IC-20260812-010 自验报告

## 结论

实现与测试已同步到指定的 S3–S4 v6 / S5 v5 基线。本地专项静态自验与用户可见硬编码扫描已通过；XCTest、未签名 IPA 与 CI 链接将在实现提交触发的 macOS CI 完成后回填。

## 输入与范围

- S3–S4 输入文件 SHA-256：`BF52BBE87692A253BDA9C2AC8B55712C76AB453E3AAF6C5D286BC15835E04C7D`。
- S5 输入文件 SHA-256：`10CD2B7829126ABBD8FB66091B21169E698868CA67E345D0EABBA39D8D6221B7`。
- 仅修改 A–E 五组要求对应的 MVE 源码、测试、本地化目录、自验门禁和交付报告。
- 未修改 S2、规格、Decision_log、Design 产物及系统标注截图占位 PNG。
- 未实现 L3 展示门槛；产品代码以 `blockedByUndecidedThreshold` 明确阻断该展示分支，没有门槛数值或以零代替。

## 实现核对

| 范围 | 结果 | 证据 |
|---|---|---|
| A：移除旧提交数量上限 | 已实现 | S3 仅保留扫描中、就绪、空集三态；非空大集合会扫描并可冻结；旧状态、迁移、守卫与四个目录条目均已移除。 |
| B：新增下游目标状态 | 已实现 | S4 定义四值域，在成功、取消、其他失败、未知终态持久化对应值后交接；S5 先读取该字段，再校验对应载荷，不读取失败类别作分流。 |
| C：新增 S5-C | 已实现 | 取消页使用三个新增 zh-Hans 条目和既有返回操作；不读取磁盘、不显示 L3、错误详情或最近删除确认操作；取消文案测试覆盖禁词。 |
| D：L3 单次读取 | 已实现 | 成功页首次进入读基线一次；用户声明时读完成值一次并持久化差值 Y；重复操作、生命周期与恢复均不补读。 |
| E：启用确认按钮 | 已实现 | 成功页在完成值尚未读取时显示并启用按钮，点击后隐藏；其他状态不显示。 |

## XCTest 数量变化

- 旧基线：136 项。
- 当前静态计数：149 项。
- 删除 16 项、增加 29 项，净增 13 项。

删除明细：

- S3 状态机删除 11 项：页面外超限入口 1 项、原状态的单项移除 3 项、全部取消 1 项、选择回落 3 项、集合清空 1 项、超限冻结守卫 1 项、选择回落守卫 1 项。
- 快照不变量删除 4 项：旧超限保留、去重后上限、旧边界冻结、选择回落顺序各 1 项。
- S5 删除 1 项：确认按钮固定禁用测试。

新增明细：

- `CollectionInvariantTests` 增加 1 项：S4 精确系统域与系统码取消分类。
- `S3StateMachineTests` 增加 1 项：大集合直接进入扫描且全部入队。
- `SnapshotInvariantTests` 增加 2 项：大集合不截断入队、大集合完成后不截断冻结。
- `S4StateMachineTests` 增加 4 项：四值域、取消分流、其余三类失败分流、交接字段持久化。
- `S5StateMachineTests` 增加 21 项：按钮启用替代测试、按交接字段直接落位、S5-C 返回与生命周期、五类禁用项、两次单读与差值持久化、重复/生命周期不补读、不可用读数、门槛阻断、载荷目标校验及取消恢复。

文件分布：

| 测试文件 | 旧数 | 新数 | 增减 |
|---|---:|---:|---:|
| CollectionInvariantTests | 15 | 16 | +1 |
| S3StateMachineTests | 32 | 22 | -10 |
| S4StateMachineTests | 38 | 42 | +4 |
| S5StateMachineTests | 25 | 45 | +20 |
| SnapshotInvariantTests | 20 | 18 | -2 |
| VolumeFormattingTests | 6 | 6 | 0 |
| 合计 | 136 | 149 | +13 |

## 自验命令与结果

```powershell
& ./Scripts/verify-IC-20260812-010.ps1
```

待最终复跑后回填：

- 专项静态自验：通过，退出码 0。
- 硬编码扫描：通过，用户可见硬编码残留 0，目录缺失 key 0、未引用 key 0。
- XCTest：待 CI 回填。
- CI：待回填。
- 未签名 IPA：待回填。
- 实现提交：待回填。
